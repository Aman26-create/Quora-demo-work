<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quora Posts</title>
    <link rel="stylesheet" href="style.css">

</head>
<body>
    <h1>Quora Posts</h1>
    <div class="main">
        <% for(let post of posts){%>
            <div>
                <h3><%= post.title %></h3>
                <p><%= post.content %></p>
                <button class="show">
                    <a href="/posts/<%= post.id %>">show in details</a>
                </button>
                <button class="edit">
                    <a href="/posts/edit/<%= post.id %>">edit</a>
                </button>
                <button type="submit">
                    <a href="/posts/del/<%=post.id%>">Delete</a>
                </button>
            </div>
        <% } %>
        <a href="http://localhost:8080/posts/new">Create a post</a>
    </div>
</body>
</html>